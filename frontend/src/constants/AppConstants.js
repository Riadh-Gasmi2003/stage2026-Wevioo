/**
 * Constantes métier partagées entre les pages, miroir des règles du backend
 * (tn.econstruction.constant.AppConstants) pour les valeurs affichées côté UI.
 */
export const AppConstants = {
  DELAI_INDIVIDUEL_JOURS: 30,
  DELAI_COLLECTIF_JOURS: 60,
  NOMBRE_MAX_RELANCES: 2,
  DELAI_RECOURS_JOURS: 30,
}
